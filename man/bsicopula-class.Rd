% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bsicopula.R
\docType{class}
\name{bsicopula-class}
\alias{bsicopula-class}
\title{Class of bivariate stochastic inversion copulas}
\description{
Couples two \linkS4class{udp} transformations through a copula on their
"carrier" uniforms \verb{(V1, V2)}. Margin \code{i}'s draw is \code{udpsi(udp_i, V_i, Z_i)}: \code{\link[=udpsi]{udpsi()}} applied to the base draw \code{V_i}, with the pre-image
selected by the randomizer \code{Z_i}. \code{Z_i} defaults to an independent
uniform, giving margins that agree with \code{basecopula} up to the
pre-image-selection randomness. When \code{randomizermod} is instead an
\linkS4class{randsdvine} or \linkS4class{randmixture} model, \verb{(Z1, Z2)} are
drawn dependently -- on \verb{(V1, V2)} and on each other -- injecting
further dependence between the two margins, including non-monotonic
dependence when \code{udp1}/\code{udp2} are many-to-one.
}
\section{Slots}{

\describe{
\item{\code{basecopula}}{a parCopula object (\pkg{copula}) or a bicop_dist object
(\pkg{rvinecopulib}), the copula of \verb{(V1, V2)}.}

\item{\code{udp1,udp2}}{objects of class \linkS4class{udp}, applied via
stochastic inversion (\code{\link[=udpsi]{udpsi()}}) to \code{V1} and \code{V2} respectively.}

\item{\code{randomizermod}}{\code{NULL} (independent randomizers), an
\linkS4class{randsdvine} object, or a \linkS4class{randmixture} object.}
}}

\references{
McNeil, A. J. and Nešlehová, J. G. (2026). Stochastic inversion of
multivariate uniform-distribution-preserving transformations.
\href{https://arxiv.org/abs/2607.07174}{arXiv:2607.07174}
}
\seealso{
\code{\link[=bsicopula]{bsicopula()}} to construct one; \code{\link[=rbsicopula]{rbsicopula()}} to sample from one.
}
