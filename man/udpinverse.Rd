% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udp-package.R, R/shuffle.R, R/udpcosine.R,
%   R/udplegendre.R, R/vtransforms.R
\name{udpinverse}
\alias{udpinverse}
\alias{udpinverse,shuffle-method}
\alias{udpinverse,udpcosine-method}
\alias{udpinverse,udplegendre-method}
\alias{udpinverse,vtransform-method}
\title{Enumerate the pre-images of a uniform-distribution-preserving transformation}
\usage{
udpinverse(x, v, prob = FALSE, ...)

\S4method{udpinverse}{shuffle}(x, v, prob = FALSE, ...)

\S4method{udpinverse}{udpcosine}(x, v, prob = FALSE, ...)

\S4method{udpinverse}{udplegendre}(x, v, prob = FALSE, ...)

\S4method{udpinverse}{vtransform}(x, v, prob = FALSE, tol = .Machine$double.eps^0.5, ...)
}
\arguments{
\item{x}{an object of class \linkS4class{udp}.}

\item{v}{a vector with values in \verb{[0, 1]}.}

\item{prob}{logical; if \code{TRUE}, attach the \code{"prob"} attribute described
above. \code{\link[=udpsi]{udpsi()}} calls \code{udpinverse()} with \code{prob = TRUE}.}

\item{...}{further arguments passed to methods; for \linkS4class{vtransform}
these are the arguments of \code{\link[=vinverse]{vinverse()}}, such as \code{method} and \code{ngrid}.}

\item{tol}{for the \linkS4class{vtransform} method, the convergence tolerance
passed to \code{\link[=vinverse]{vinverse()}}.}
}
\value{
A numeric matrix, \code{length(v)} by \code{k}, of pre-images: leading entries
filled, trailing entries \code{NA}, each row sorted ascending; optionally with
a \code{"prob"} attribute.
}
\description{
Returns every pre-image of each element of \code{v} under a \code{\link[=udptrans]{udptrans()}} map,
packed into a matrix. Because the map is in general many-to-one, each \code{v}
may have several pre-images; \code{udpinverse()} returns them all.
}
\details{
The number of pre-images depends on the class of \code{x}:
\itemize{
\item \linkS4class{shuffle}: exactly one (the map is a bijection).
\item \linkS4class{vtransform}: two -- the lower-branch pre-image (\code{\link[=vinverse]{vinverse()}})
in column 1 and the upper-branch one in column 2. At \code{v} equal to \code{0} or
\code{1} the branches coincide and both columns hold the same value.
\item \linkS4class{udpcosine}: up to \code{degree}, one per linear piece; \code{v} equal
to \code{0} or \code{1} has fewer (the shared troughs and peaks).
}

The result is a numeric matrix with \code{length(v)} rows and \code{k} columns, \code{k}
being the largest number of pre-images any \code{v} can have for this \code{x}. Row
\code{i} holds the pre-images of \code{v[i]} in its leading entries, sorted ascending,
with \code{NA} in the trailing entries when \code{v[i]} has fewer than \code{k} of them.
The count for row \code{i} is \code{sum(!is.na(result[i, ]))}.

With \code{prob = TRUE} the result also carries an \code{n}-by-\code{k} \code{"prob"} attribute,
aligned column-for-column with the matrix: \code{attr(., "prob")[i, j]} is the
probability with which \code{\link[=udpsi]{udpsi()}} selects pre-image \code{result[i, j]}, namely
\verb{1 / |T'|} at that pre-image normalised over the row (\code{NA} where the
pre-image is \code{NA}). On the measure-zero set where \verb{T'} is undefined at some
pre-image the row falls back to equal probabilities.

\code{v} is treated as a plain numeric vector; unlike \code{\link[=udptrans]{udptrans()}} and \code{\link[=udpsi]{udpsi()}},
\code{udpinverse()} does not copy the attributes of \code{v} onto its result (the row
dimension is \code{length(v)}, so it cannot).
}
\section{Functions}{
\itemize{
\item \code{udpinverse(shuffle)}: Pre-image of a shuffle: a one-column matrix holding
the single pre-image \code{\link[=shinverse]{shinverse()}} of each \code{v}. With \code{prob = TRUE} the
\code{"prob"} attribute is identically \code{1}.

\item \code{udpinverse(udpcosine)}: Pre-images of a cosine udp transformation: a matrix
with \code{degree} columns holding the roots \code{\link[=udpcosinverse]{udpcosinverse()}} of each \code{v},
sorted ascending and left-packed, \code{NA}-padded where \code{v} equal to \code{0} or
\code{1} has fewer than \code{degree} roots. With \code{prob = TRUE} the \code{"prob"}
attribute is equal over each row's roots.

\item \code{udpinverse(udplegendre)}: Pre-images of a shifted-Legendre udp transformation:
a matrix with \code{degree} columns holding, for each \code{v}, the roots in
\verb{[0, 1]} of \code{L_j(u) = F_j^{-1}(v)}, sorted ascending and left-packed with
trailing \code{NA}. With \code{prob = TRUE} the \code{"prob"} attribute weights each root
by \verb{1 / |L_j'(u)|}, normalised over the row.

\item \code{udpinverse(vtransform)}: Pre-images of a v-transform: a two-column matrix with
the lower-branch pre-image \code{\link[=vinverse]{vinverse()}} in column 1 and \code{v} plus that in
column 2 (the two coincide at \code{v = 0} and \code{v = 1}). With \code{prob = TRUE},
column 1 of the \code{"prob"} attribute is the conditional down-probability
\code{\link[=vdownprob]{vdownprob()}}. Accepts \code{tol} and further arguments of \code{\link[=vinverse]{vinverse()}}.

}}
\examples{
udpinverse(shuffle(c(2, 1, 3)), c(0.2, 0.5, 0.9))
udpinverse(vsymmetric(), c(0, 0.25, 0.5, 0.75, 1))
udpinverse(udpcosine(3), c(0, 0.4, 1))
udpinverse(vlinear(0.4), c(0.2, 0.6), prob = TRUE)
}
