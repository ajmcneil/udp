% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udpzigzag.R
\name{udpzigzag}
\alias{udpzigzag}
\title{Construct a zigzag udp transformation}
\usage{
udpzigzag(breaks = NULL, widths = NULL, up = TRUE)
}
\arguments{
\item{breaks}{the interior breakpoints (\code{0} and \code{1} excluded), strictly
increasing and strictly inside \verb{(0, 1)}; an empty vector gives a single
piece. Mutually exclusive with \code{widths}.}

\item{widths}{positive piece widths, in order; any positive values are
accepted and normalized to sum to \code{1}. Mutually exclusive with \code{breaks}.}

\item{up}{logical; does the first piece increase? Defaults to \code{TRUE}.}
}
\value{
An object of class \linkS4class{udpzigzag}.
}
\description{
Specify the pieces either by their interior boundaries (\code{breaks}) or by
their widths (\code{widths}); exactly one of the two must be given.
}
\details{
A single piece (\code{breaks = numeric(0)} or \code{widths} of length \code{1}) reduces
to \code{\link{udpid}()} (\code{up = TRUE}) or \code{\link{udpflip}()}
(\code{up = FALSE}). Equal widths reduce to \code{udpcosine(n)}, \code{n} the number of
pieces, matching \linkS4class{udpcosine}'s own starting direction with
\code{up = TRUE} for odd \code{n} and \code{up = FALSE} for even \code{n}. Two pieces reduce
exactly to \code{vlinear(delta)} with \code{widths = c(delta, 1 - delta)},
\code{up = FALSE}.
}
\examples{
udpzigzag(breaks = c(0.3, 0.5))
udpzigzag(widths = c(1, 2, 1))
udpzigzag(widths = c(0.4, 0.6), up = FALSE) # same map as vlinear(0.4)
}
