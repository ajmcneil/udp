% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bsicopula.R
\name{randmixture}
\alias{randmixture}
\title{Construct a mixture-of-copulas randomizer model}
\usage{
randmixture(cop1, cop2, selector)
}
\arguments{
\item{cop1, cop2}{parCopula objects (\pkg{copula}) or bicop_dist objects
(\pkg{rvinecopulib}), the two copulas of \verb{(Z1, Z2)} to choose between.}

\item{selector}{a function \code{selector(v1, v2)} returning a logical vector
the same length as \code{v1}/\code{v2}, with no \code{NA}s: \code{TRUE} selects \code{cop1},
\code{FALSE} selects \code{cop2}. Capture any further parameters in its closure,
e.g. \code{function(v1, v2) pmax(v1, v2) > 0.7}. Must be pure and
vectorized -- see \linkS4class{randmixture}'s Details.}
}
\value{
An object of class \linkS4class{randmixture}.
}
\description{
Construct a mixture-of-copulas randomizer model
}
\examples{
if (requireNamespace("rvinecopulib", quietly = TRUE)) {
  randmixture(
    rvinecopulib::bicop_dist("gaussian", 0, 1),
    rvinecopulib::bicop_dist("gaussian", 0, -1),
    selector = function(v1, v2) pmax(v1, v2) > 0.7
  )
}
}
