% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bsicopula.R
\name{rbsicopula}
\alias{rbsicopula}
\title{Random sample from a bivariate stochastic inversion copula}
\usage{
rbsicopula(n, object)
}
\arguments{
\item{n}{number of draws.}

\item{object}{an object of class \linkS4class{bsicopula}.}
}
\value{
A numeric matrix with \code{n} rows and columns \code{U1}, \code{U2}, each
marginally uniform on \verb{[0, 1]}.
}
\description{
\enumerate{
\item Draws \verb{(V1, V2)} from \code{basecopula}.
\item If \code{randomizermod} is \code{NULL}, returns \verb{(udpsi(udp1, V1), udpsi(udp2, V2))} -- each with its own fresh, independent randomizer -- and stops.
\item Otherwise draws \verb{(Z1, Z2)} given \verb{(V1, V2)} from the simplified D-vine
specified by \code{basecopula} (as \verb{C_\{V1,V2\}}) and \code{randomizermod}.
\item Returns \verb{(udpsi(udp1, V1, Z1), udpsi(udp2, V2, Z2))}.
}
}
\examples{
if (requireNamespace("rvinecopulib", quietly = TRUE)) {
  set.seed(1)
  bc <- bsicopula(rvinecopulib::bicop_dist("gaussian", 0, 0.5), udpcosine(2), udpcosine(3))
  rbsicopula(1000, bc)
}
}
