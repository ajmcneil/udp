% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udp-package.R, R/udplegendre.R
\name{udpcdf}
\alias{udpcdf}
\alias{udpcdf,udplegendre-method}
\title{Distribution function of the underlying variable of a udp transformation}
\usage{
udpcdf(x, y)

\S4method{udpcdf}{udplegendre}(x, y)
}
\arguments{
\item{x}{an object of class \linkS4class{udp}.}

\item{y}{a numeric vector, raw values in the range of \code{g}.}
}
\value{
A numeric vector of probabilities in \verb{[0, 1]}, the same length as \code{y}.
}
\description{
Some \linkS4class{udp} transformations are built as a composition \code{T(u) = F(g(u))} of an underlying function \code{g} (for instance a polynomial) with the
distribution function \code{F} of \code{g(U)} for \code{U} uniform on \verb{[0, 1]} -- see
\linkS4class{udplegendre}. \code{udpcdf()} evaluates that \code{F} directly, at raw
values in the range of \code{g}, rather than composed with \code{g} the way
\code{\link[=udptrans]{udptrans()}} evaluates \code{T}. Only classes built this way have a method; it
is not meaningful for every \linkS4class{udp} class (a \linkS4class{shuffle},
for instance, has no such decomposition).
}
\section{Methods (by class)}{
\itemize{
\item \code{udpcdf(udplegendre)}: \code{F_j}, the distribution function of \code{L_j(U)}.

}}
\examples{
udpcdf(udplegendre(3), c(-0.5, 0, 0.5))
}
