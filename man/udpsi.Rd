% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udp-package.R
\name{udpsi}
\alias{udpsi}
\alias{udpsi,udp-method}
\title{Stochastically invert a uniform-distribution-preserving transformation}
\usage{
udpsi(x, v, Z = runif(length(v)), ...)

\S4method{udpsi}{udp}(x, v, Z = runif(length(v)), ...)
}
\arguments{
\item{x}{an object of class \linkS4class{udp}.}

\item{v}{a vector, matrix or time series with values in \verb{[0, 1]}.}

\item{Z}{a vector of randomizers with values in \verb{[0, 1]}, the same length as
\code{v}; defaults to a fresh draw from \code{\link[stats:runif]{stats::runif()}}. Ignored when \code{x} is a
\linkS4class{shuffle}.}

\item{...}{further arguments forwarded to \code{\link[=udpinverse]{udpinverse()}}; for
\linkS4class{vtransform} these are the arguments of \code{\link[=vinverse]{vinverse()}}, such as
\code{method}, \code{tol} and \code{ngrid}.}
}
\value{
An object shaped like \code{v} with values in \verb{[0, 1]}.
}
\description{
A \code{\link[=udptrans]{udptrans()}} map is in general many-to-one, so it has no ordinary inverse.
\code{udpsi()} returns a single pre-image of \code{v}, drawn so that the
uniform-distribution-preserving property runs backwards too: if \code{v} is
uniform on \verb{[0, 1]} and \code{Z} is an independent uniform, then \code{udpsi(x, v, Z)}
is uniform on \verb{[0, 1]}. \code{Z} carries the randomization.
}
\details{
For every \linkS4class{udp} class this is the same computation: \code{\link[=udpinverse]{udpinverse()}}
enumerates the pre-images of each \code{v} together with their selection
probabilities (\verb{1 / |T'|} at each pre-image, normalized over the row), and
\code{udpsi()} picks one pre-image per \code{v} by inverse-CDF sampling against \code{Z}.
A \linkS4class{shuffle} has a single pre-image and \code{Z} is ignored;
\linkS4class{udpcosine} weights its \code{degree} pre-images equally;
\linkS4class{vtransform} splits between its two branches by the conditional
down-probability \code{\link[=vdownprob]{vdownprob()}}.
}
\section{Functions}{
\itemize{
\item \code{udpsi(udp)}: Draw one pre-image of each \code{v} by inverse-CDF sampling of
\code{\link[=udpinverse]{udpinverse()}} against \code{Z}. This one method serves every
\linkS4class{udp} class.

}}
\examples{
udpsi(vsymmetric(), c(0, 0.25, 0.5, 0.75, 1))
udpsi(shuffle(c(2, 1, 3)), c(0.1, 0.5, 0.9))
x <- udpcosine(3)
set.seed(1)
udpsi(x, udptrans(x, runif(5)))
}
