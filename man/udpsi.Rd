% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udp-package.R, R/shuffle.R, R/udpcosine.R,
%   R/vtransforms.R
\name{udpsi}
\alias{udpsi}
\alias{udpsi,shuffle-method}
\alias{udpsi,udpcosine-method}
\alias{udpsi,vtransform-method}
\title{Stochastically invert a uniform-distribution-preserving transformation}
\usage{
udpsi(x, v, Z = runif(length(v)), ...)

\S4method{udpsi}{shuffle}(x, v, Z = runif(length(v)), ...)

\S4method{udpsi}{udpcosine}(x, v, Z = runif(length(v)), ...)

\S4method{udpsi}{vtransform}(x, v, Z = runif(length(v)), tol = .Machine$double.eps^0.5, ...)
}
\arguments{
\item{x}{an object of class \linkS4class{udp}.}

\item{v}{a vector, matrix or time series with values in \verb{[0, 1]}.}

\item{Z}{a vector of randomisers with values in \verb{[0, 1]}, the same length as
\code{v}; defaults to a fresh draw from \code{\link[stats:runif]{stats::runif()}}. Ignored when \code{x} is a
\linkS4class{shuffle}.}

\item{...}{further arguments passed to methods; for \linkS4class{vtransform}
these are the arguments of \code{\link[=vinverse]{vinverse()}}, such as \code{method}.}

\item{tol}{for the \linkS4class{vtransform} method, the convergence tolerance
passed to \code{\link[=vinverse]{vinverse()}}.}
}
\value{
An object shaped like \code{v} with values in \verb{[0, 1]}.
}
\description{
A \code{\link[=udptrans]{udptrans()}} map is in general many-to-one, so it has no ordinary inverse.
\code{udpsi()} returns a single pre-image of \code{v}, drawn so that the
uniform-distribution-preserving property runs backwards too: if \code{v} is
uniform on \verb{[0, 1]} and \code{Z} is an independent uniform, then \code{udpsi(x, v, Z)}
is uniform on \verb{[0, 1]}. \code{Z} carries the randomisation.
}
\details{
The choice of pre-image depends on the class of \code{x}:
\itemize{
\item \linkS4class{vtransform}: the lower pre-image (at or below the fulcrum) is
returned with probability equal to the conditional down-probability
\code{\link[=vdownprob]{vdownprob()}}, the upper one otherwise.
\item \linkS4class{udpcosine}: one of the \code{degree} pre-images is returned, root
number \code{ceiling(Z * degree)} counting from the left.
\item \linkS4class{shuffle}: a shuffle is a bijection, so its inverse is
deterministic (\code{\link[=shinverse]{shinverse()}}) and \code{Z} is ignored.
}
}
\section{Functions}{
\itemize{
\item \code{udpsi(shuffle)}: Invert a shuffle. A shuffle is a bijection, so this is the
deterministic inverse \code{\link[=shinverse]{shinverse()}} and \code{Z} is ignored.

\item \code{udpsi(udpcosine)}: Stochastic inverse of a cosine udp transformation. For each
\code{v} in \verb{(0, 1)}, \code{\link[=udpcosinverse]{udpcosinverse()}} returns \code{degree} pre-images and the
selected one is root number \code{ceiling(Z * degree)}; \code{v} equal to \code{0} or \code{1}
returns \code{0}.

\item \code{udpsi(vtransform)}: Stochastic inverse of a v-transform. Accepts \code{tol} and
further arguments of \code{\link[=vinverse]{vinverse()}} (such as \code{method}).

}}
\examples{
udpsi(vsymmetric(), c(0, 0.25, 0.5, 0.75, 1))
udpsi(shuffle(c(2, 1, 3)), c(0.1, 0.5, 0.9))
x <- udpcosine(3)
set.seed(1)
udpsi(x, udptrans(x, runif(5)))
}
