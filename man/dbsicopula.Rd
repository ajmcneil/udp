% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bsicopula.R
\name{dbsicopula}
\alias{dbsicopula}
\title{Density of a bivariate stochastic inversion copula}
\usage{
dbsicopula(u1, u2, object)
}
\arguments{
\item{u1, u2}{numeric vectors of equal length with values in \verb{[0, 1]}.}

\item{object}{an object of class \linkS4class{bsicopula}.}
}
\value{
A numeric vector, the same length as \code{u1}, of density values.
}
\description{
The population density of a \linkS4class{bsicopula}. When \code{randomizermod}
is \code{NULL} (independent stochastic inversion), this is simply
\code{c_V(udptrans(udp1, u1), udptrans(udp2, u2))}, \code{c_V} being the density of
\code{basecopula}: \code{udpsi()} always returns an exact pre-image, and the
\verb{1 / |T'|} Jacobian of the change of variables cancels exactly against
the \verb{1 / |T'|} pre-image selection probability, since a
\linkS4class{udp} transformation's pre-image selection probabilities sum
to \code{1} at every \code{v} by construction.
}
\details{
When \code{randomizermod} is an \linkS4class{sdvine}, that cancellation is no
longer exact -- \code{Z1} now depends on \code{V2} (and vice versa) through the
D-vine, not just on its own \code{V_i} -- and an extra multiplicative weight
\code{w(u1, u2)} corrects for it: the ratio of the probability that the joint
stochastic inverse lands in the same pair of pre-image branches as
\verb{(u1, u2)}, given \verb{(V1, V2)}, under \code{randomizermod}, to the same
probability under independent randomizers. \code{w} is \code{1} identically when
\code{randomizermod} is \code{NULL}.

\code{udp1} and \code{udp2} are each piecewise smooth with finitely many
breakpoints; at a breakpoint of either, the number of
pre-image branches drops (two branches meet at the same point), so the
density has a genuine jump discontinuity there -- harmless for a density
(a finite set of points has measure zero) but worth knowing about if
evaluating \code{dbsicopula()} on a grid that happens to include one:
\code{udptrans()}/\code{udpinverse()} are recomputed fresh from \code{u1}, \code{u2} at every
call, so the value returned at a breakpoint is the (well-defined) value
for the merged branch there, not an arbitrary one-sided limit.
}
\examples{
if (requireNamespace("rvinecopulib", quietly = TRUE)) {
  bc <- bsicopula(rvinecopulib::bicop_dist("gaussian", 0, 0.5), udpcosine(2), udpcosine(3))
  dbsicopula(c(0.2, 0.5), c(0.3, 0.5), bc)
}
}
\references{
McNeil, A. J. and Nešlehová, J. G. (2026). Stochastic inversion of
multivariate uniform-distribution-preserving transformations.
\href{https://arxiv.org/abs/2607.07174}{arXiv:2607.07174}
}
