% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shuffle.R
\name{plot,bishuffle,missing-method}
\alias{plot,bishuffle,missing-method}
\title{Plot method for the bishuffle class}
\usage{
\S4method{plot}{bishuffle,missing}(x, xlab = "u", ylab = NULL, embellish = c("none", "colour", "bw"), ...)
}
\arguments{
\item{x}{an object of class \linkS4class{bishuffle}.}

\item{xlab}{x-axis label, shared by both panels.}

\item{ylab}{y-axis label(s) for the \code{shuffle1} and \code{shuffle2} panels
respectively; a single value is recycled for both.}

\item{embellish}{style of the strip-boundary gridlines in each panel,
forwarded to \link{plot,shuffle,missing-method}: \code{"none"} (the default),
\code{"colour"}, or \code{"bw"}.}

\item{...}{further graphical parameters passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
No return value, generates a plot.
}
\description{
Draws the two shuffles of a fitted \linkS4class{bishuffle} object side by
side, in the manner of \code{\link[graphics:plot]{graphics::plot()}}, sharing a common \verb{[0, 1]}
frame (every \linkS4class{shuffle} plot already does).
}
\examples{
set.seed(1)
u1 <- runif(2000)
u2 <- (u1 + 0.5) \%\% 1
fit <- aceshuffle(cbind(u1, u2), m = 4)
plot(fit, embellish = "colour")
}
