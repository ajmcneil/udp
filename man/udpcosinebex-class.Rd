% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udpcosinebex.R
\docType{class}
\name{udpcosinebex-class}
\alias{udpcosinebex-class}
\title{Class of finite orthonormal-cosine-expansion udp transformations}
\description{
A udpcosinebex transformation is \code{T(u) = F(g(u))}, the composition of
\verb{g(u) = sum_j coef[j] * Omega_j(u)} -- a finite expansion in the orthonormal
cosine functions \code{Omega_j(u) = sqrt(2) * (-1)^j * cos(j * pi * u)} on
\verb{[0, 1]} -- with the distribution function \code{F} of \code{g(U)} for \code{U} uniform.
It is a uniform-distribution-preserving map of \verb{[0, 1]}: for \code{U} uniform,
\code{udptrans(x, U)} is again uniform. \linkS4class{udpcosine} is the one-term
special case; see \code{\link[=udpcosinebex]{udpcosinebex()}} for how the two relate.
}
\details{
The identity \code{cos(j*theta) = T_j(cos(theta))} turns \code{g} into an ordinary
polynomial \verb{h(x) = sqrt(2) * sum_j coef[j] * (-1)^j * T_j(x)} in the
DIFFERENT variable \code{x = cos(pi * u)}, with \code{g(u) = h(cos(pi * u))}. \code{F} and
its inverse are built exactly as for \linkS4class{udplegendrebex} --
exact real roots from polynomial root finding, panel splines of \code{F} and
\code{F^{-1}} between consecutive turning-point critical values, regularized
against the square-root branch point at each -- but working in \code{x}, with
results converted back to \code{u} via \code{u = acos(x) / pi}.
}
\section{Slots}{

\describe{
\item{\code{coef}}{numeric; the weights on \verb{Omega_1, ..., Omega_degree}, trailing
zeros trimmed.}

\item{\code{degree}}{integer; the index of the last nonzero weight.}

\item{\code{cfs,cfsD}}{ascending monomial coefficients of \code{h} (a polynomial in \code{x},
not \code{u}) and of its derivative.}

\item{\code{lbound,ubound}}{the range of \code{g} (equivalently of \code{h}) on \verb{[0, 1]}
(equivalently on \verb{[-1, 1]}).}

\item{\code{Tfun,Qfun}}{functions evaluating \code{T} and \code{F^{-1}}.}
}}

\references{
McNeil, A. J., Nešlehová, J. G. and Smith, A. D. (2025). Measures and models
of non-monotonic dependence. \href{https://arxiv.org/abs/2512.10828}{arXiv:2512.10828}
}
\seealso{
\code{\link[=udpcosinebex]{udpcosinebex()}} to construct one, \code{\link[=udptrans]{udptrans()}} to evaluate it.
}
