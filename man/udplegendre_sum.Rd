% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udplegendre_sum.R
\name{udplegendre_sum}
\alias{udplegendre_sum}
\title{Construct a finite orthonormal-Legendre-expansion udp transformation}
\usage{
udplegendre_sum(coef, ngrid = 513L)
}
\arguments{
\item{coef}{a numeric vector of weights, \code{coef[j]} multiplying the
orthonormal shifted Legendre polynomial \verb{P_j(u) = sqrt(2j + 1) L_j(u)}.
Trailing zeros are trimmed to find the effective degree; at least one
entry must be nonzero. There is no \code{j = 0} (constant) term: \code{T(u) = F(g(u))} is unchanged by an additive shift of \code{g}, so a constant term
would carry no information.}

\item{ngrid}{number of grid points (at least 3) at which \code{F} is evaluated
exactly before interpolation.}
}
\value{
An object of class \linkS4class{udplegendre_sum}.
}
\description{
Construct a finite orthonormal-Legendre-expansion udp transformation
}
\details{
Because \code{P_j} is a positive rescaling of \code{L_j}, and \verb{T(u) = F_\{g(U)\}(g(u))}
is unchanged by rescaling \code{g} by any positive constant, a one-hot \code{coef}
with a single positive entry reproduces the corresponding
\linkS4class{udplegendre} exactly: \code{udptrans(udplegendre_sum(c(0, 0, 1)), u)} equals \code{udptrans(udplegendre(3), u)}. Flipping the sign of every entry
of \code{coef} (equivalently, negating \code{g}) replaces \code{T} with its complement,
\code{1 - T}.

Unlike \linkS4class{udplegendre}, no closed form is used for any effective
degree, including 1 or 2: every \code{coef} goes through the same panel-spline
construction, for \code{\link[=udplegendre]{udplegendre()}} itself if the exact, fast single-term
closed forms are wanted.

Panel boundaries are placed at the critical values of \code{g}'s turning points,
as for \linkS4class{udplegendre}, and additionally at \code{g(0)} and \code{g(1)}
whenever either lies strictly inside the range: a pure \code{L_j} always
attains \code{lbound} or \code{1} exactly at \code{u = 0} or \code{u = 1}, but a general
combination need not, and wherever \code{g(0)} or \code{g(1)} sits strictly between
\code{lbound} and \code{ubound}, \code{F} has a genuine (finite-slope) kink there -- a
second branch starts or stops contributing at that value. Splitting the
panel there, rather than asking one spline to interpolate through the
kink, is what keeps accuracy close to \linkS4class{udplegendre}'s.
}
\examples{
udplegendre_sum(c(0.44, -0.35, 0.56, -0.21, 0.36))
plot(udplegendre_sum(c(0.44, -0.35, 0.56, -0.21, 0.36)), embellish = "colour")
}
