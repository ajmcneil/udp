% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udplegendre.R
\docType{class}
\name{udplegendre-class}
\alias{udplegendre-class}
\title{Class of shifted-Legendre udp transformations}
\description{
A shifted-Legendre udp transformation is \code{T(u) = F_j(L_j(u))}, the
composition of the shifted Legendre polynomial \code{L_j} of the stated degree
\code{j} on \verb{[0, 1]} with the distribution function \code{F_j} of \code{L_j(U)} for \code{U}
uniform. It is a degree-\code{j}, uniform-distribution-preserving map of
\verb{[0, 1]}: for \code{U} uniform, \code{udptrans(x, U)} is again uniform.
}
\details{
\code{F_j} and its inverse have no closed form for \code{degree} above 2, so the
constructor evaluates \code{F_j} exactly on a grid once (from the real roots of
\code{L_j(u) = y}) and stores spline interpolations, \code{Tfun} for \code{T} itself and
\code{Qfun} for the quantile \code{F_j^{-1}}. \code{F_j} has a square-root branch point at
every interior extreme value of \code{L_j}; the splines are built panel by panel
between those values under the substitution that regularizes them, so the
interpolation error stays near \code{1e-4} across the usable degree range.
Pre-image finding in \code{\link[=udpinverse]{udpinverse()}} stays exact (polynomial root finding);
only the \code{F_j} reparametrization is interpolated.
}
\section{Slots}{

\describe{
\item{\code{degree}}{integer; the degree of the transformation (at least 1).}

\item{\code{cfs,cfsD}}{ascending monomial coefficients of \code{L_j} and of its
derivative.}

\item{\code{lbound}}{lower end of the range of \code{L_j} on \verb{[0, 1]}.}

\item{\code{Tfun,Qfun}}{functions evaluating \code{T} and \code{F_j^{-1}}.}
}}

\references{
McNeil, A. J., Nešlehová, J. G. and Smith, A. D. (2025). Measures and models
of non-monotonic dependence. \href{https://arxiv.org/abs/2512.10828}{arXiv:2512.10828}
}
\seealso{
\code{\link[=udplegendre]{udplegendre()}} to construct one, \code{\link[=udptrans]{udptrans()}} to evaluate it.
}
