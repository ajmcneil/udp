% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udplegendre_sum.R
\docType{class}
\name{udplegendre_sum-class}
\alias{udplegendre_sum-class}
\title{Class of finite orthonormal-Legendre-expansion udp transformations}
\description{
A udplegendre_sum transformation is \code{T(u) = F(g(u))}, the composition of
\verb{g(u) = sum_j coef[j] * P_j(u)} -- a finite expansion in the orthonormal
shifted Legendre polynomials \verb{P_j(u) = sqrt(2j + 1) L_j(u)} on \verb{[0, 1]} --
with the distribution function \code{F} of \code{g(U)} for \code{U} uniform. It is a
uniform-distribution-preserving map of \verb{[0, 1]}: for \code{U} uniform,
\code{udptrans(x, U)} is again uniform. \linkS4class{udplegendre} is the
one-term special case; see \code{\link[=udplegendre_sum]{udplegendre_sum()}} for how the two relate.
}
\details{
\code{F} and its inverse are constructed exactly as for \linkS4class{udplegendre}
-- exact real roots of \code{g(u) = y} from \code{\link[=udpinverse]{udpinverse()}}'s polynomial root
finder, panel splines of \code{F} and \code{F^{-1}} between consecutive turning-point
values of \code{g}, regularized against the square-root branch point at each --
except that the range of \code{g} need not be \verb{[lbound, 1]}, so both ends of the
range are carried as separate slots.
}
\section{Slots}{

\describe{
\item{\code{coef}}{numeric; the weights on \verb{P_1, ..., P_degree}, trailing zeros
trimmed.}

\item{\code{degree}}{integer; the index of the last nonzero weight.}

\item{\code{cfs,cfsD}}{ascending monomial coefficients of \code{g} and of its
derivative.}

\item{\code{lbound,ubound}}{the range of \code{g} on \verb{[0, 1]}.}

\item{\code{Tfun,Qfun}}{functions evaluating \code{T} and \code{F^{-1}}.}
}}

\references{
McNeil, A. J., Nešlehová, J. G. and Smith, A. D. (2025). Measures and models
of non-monotonic dependence. \href{https://arxiv.org/abs/2512.10828}{arXiv:2512.10828}
}
\seealso{
\code{\link[=udplegendre_sum]{udplegendre_sum()}} to construct one, \code{\link[=udptrans]{udptrans()}} to evaluate it.
}
