% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udpcosine.R
\name{udpcosinesi}
\alias{udpcosinesi}
\title{Stochastic inverse of a cosine udp transformation}
\usage{
udpcosinesi(x, v, Z = runif(length(v)))
}
\arguments{
\item{x}{an object of class \linkS4class{udpcosine}.}

\item{v}{a vector, matrix or time series with values in \verb{[0, 1]}.}

\item{Z}{a vector of randomisers with values in \verb{[0, 1]}, the same length as
\code{v}; defaults to a fresh draw from \code{\link[stats:runif]{stats::runif()}}.}
}
\value{
An object shaped like \code{v} with values in \verb{[0, 1]}.
}
\description{
For each \code{v} in \verb{(0, 1)}, \code{\link[=udpcosinverse]{udpcosinverse()}} returns \code{degree} pre-images;
\code{udpcosinesi()} picks one of them uniformly at random. \code{Z} is the
randomiser: the selected pre-image is the \code{Z[i]}-quantile of the equiprobable
categorical distribution over the pre-images, i.e. root number
\code{ceiling(Z[i] * degree)}. Values of \code{v} equal to \code{0} or \code{1} return \code{0}.
}
\details{
If \code{v} is uniform on \verb{[0, 1]} and \code{Z} is an independent uniform, the result
is again uniform on \verb{[0, 1]}.
}
\examples{
x <- udpcosine(3)
set.seed(1)
u <- runif(5)
udpcosinesi(x, udpcostrans(x, u))
}
