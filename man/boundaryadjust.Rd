% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{boundaryadjust}
\alias{boundaryadjust}
\title{Force Values Strictly Inside (0, 1)}
\usage{
boundaryadjust(u, btol = NA)
}
\arguments{
\item{u}{a numeric vector, matrix or array with values in \verb{[0, 1]}.}

\item{btol}{boundary tolerance. If \code{NA} (the default), \code{1 / (2 * n)} is
used, where \code{n} is the number of rows of \code{u} if \code{u} is a matrix (one
tolerance per observation, however many columns it has) and
\code{length(u)} otherwise.}
}
\value{
\code{u} with any values exactly at \code{0} or \code{1} replaced so that all
values lie strictly inside \verb{(0, 1)}.
}
\description{
Nudges any values exactly at the boundary of \verb{[0, 1]} strictly inside,
replacing \code{0} by \code{btol} and \code{1} by \code{1 - btol}. Useful whenever a fitted
model or transformation produces values that should lie in the open
interval \verb{(0, 1)} but can attain the closed boundary exactly -- for
example the output of \code{\link[=udptrans]{udptrans()}}, or of a marginal distribution fitted
to data and then evaluated at the data themselves.
}
\examples{
boundaryadjust(c(0, 0.3, 1))
boundaryadjust(matrix(c(0, 0.4, 1, 0.7), 2, 2))
}
