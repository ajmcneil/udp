% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udplegendrebex.R
\name{plot,udplegendrebex,missing-method}
\alias{plot,udplegendrebex,missing-method}
\title{Plot method for the udplegendrebex class}
\usage{
\S4method{plot}{udplegendrebex,missing}(
  x,
  n = 500L,
  xlab = "u",
  ylab = "T(u)",
  embellish = c("none", "colour", "bw"),
  ...
)
}
\arguments{
\item{x}{an object of class \linkS4class{udplegendrebex}.}

\item{n}{number of points at which to evaluate the transformation.}

\item{xlab, ylab}{axis labels.}

\item{embellish}{style of the gridlines: \code{"none"} (the default) to omit
them, \code{"colour"} for red, or \code{"bw"} for grey.}

\item{...}{further graphical parameters passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
No return value, generates a plot.
}
\description{
Draws the graph of the udplegendrebex transformation over thin gridlines
marking its A-partition and T-partition: vertical lines at the points of
\code{udpbreaks()} (the A-partition, \code{0} and \code{1} excluded as redundant with the
plot's own border), the coarsest partition of \verb{[0, 1]} into intervals on
which \code{T} is continuously differentiable; horizontal lines at the distinct
values \code{T} takes at those points (the T-partition), \code{0} and \code{1} again
excluded. A point with a one-sided vertical tangent but no fold reaches
the same \code{T} value as the turning point whose critical value it shares, so
it contributes a vertical line without adding a new horizontal one; \code{u = 0}
and \code{u = 1} are always A-partition members but need not be trivial
T-partition values, so they are handled separately in each role.
}
\examples{
plot(udplegendrebex(c(0.44, -0.35, 0.56, -0.21, 0.36)), embellish = "colour")
plot(udplegendrebex(c(0, 0.6, 0, -0.4)), embellish = "bw")
}
