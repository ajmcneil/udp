% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udpzigzag.R
\name{plot,udpzigzag,missing-method}
\alias{plot,udpzigzag,missing-method}
\title{Plot method for the udpzigzag class}
\usage{
\S4method{plot}{udpzigzag,missing}(x, xlab = "u", ylab = "T(u)", embellish = c("none", "colour", "bw"), ...)
}
\arguments{
\item{x}{an object of class \linkS4class{udpzigzag}.}

\item{xlab, ylab}{axis labels.}

\item{embellish}{style of the kink gridlines: \code{"none"} (the default) to
omit them, \code{"colour"} for red, or \code{"bw"} for grey.}

\item{...}{further graphical parameters passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
No return value, generates a plot.
}
\description{
Draws the graph of the zigzag udp transformation by connecting its
piece-boundary values directly -- the map is exactly piecewise linear, so
no intermediate grid is needed.
}
\examples{
plot(udpzigzag(widths = c(1, 2, 1)), embellish = "colour")
plot(udpzigzag(breaks = c(0.3, 0.5), up = FALSE), embellish = "bw")
}
