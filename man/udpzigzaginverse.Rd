% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udpzigzag.R
\name{udpzigzaginverse}
\alias{udpzigzaginverse}
\title{Roots of a zigzag udp transformation}
\usage{
udpzigzaginverse(x, v)
}
\arguments{
\item{x}{an object of class \linkS4class{udpzigzag}.}

\item{v}{a vector with values in \verb{[0, 1]}.}
}
\value{
A list the same length as \code{v}; element \code{j} is a list with \code{u}, the
sorted distinct roots of \code{udptrans(x, u) == v[j]}, and \code{w}, the matching
summed piece widths.
}
\description{
\code{udptrans()} is an unequal-width triangle wave and so is not injective: a
value \code{v} in \verb{(0, 1)} has one pre-image per piece, while \code{v = 0} and
\code{v = 1} have fewer (pieces sharing a trough or peak breakpoint collapse to
the same root). Each root carries the width of every piece that collapsed
onto it, for use as the raw \verb{1 / |T'|} selection weight in
\code{\link[=udpinverse]{udpinverse()}}.
}
\keyword{internal}
