% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udpcosine_sum.R
\name{udpcosine_sum}
\alias{udpcosine_sum}
\title{Construct a finite orthonormal-cosine-expansion udp transformation}
\usage{
udpcosine_sum(coef, ngrid = 513L)
}
\arguments{
\item{coef}{a numeric vector of weights, \code{coef[j]} multiplying the
orthonormal cosine function \code{Omega_j(u) = sqrt(2) * (-1)^j * cos(j * pi * u)}. Trailing zeros are trimmed to find the effective degree; at least
one entry must be nonzero. There is no \code{j = 0} (constant) term, for the
same reason as \linkS4class{udplegendre_sum}.}

\item{ngrid}{number of grid points (at least 3) at which \code{F} is evaluated
exactly before interpolation.}
}
\value{
An object of class \linkS4class{udpcosine_sum}.
}
\description{
Construct a finite orthonormal-cosine-expansion udp transformation
}
\details{
A one-hot \code{coef} reproduces the corresponding \linkS4class{udpcosine}
exactly, up to the \code{1 / sqrt(2)} rescaling needed to undo the orthonormal
\code{sqrt(2)} factor: \code{udptrans(udpcosine_sum(c(0, 0, 1 / sqrt(2))), u)} equals
\code{udptrans(udpcosine(3), u)}. As for \linkS4class{udplegendre_sum}, flipping
the sign of every entry of \code{coef} replaces \code{T} with its complement, \code{1 - T}.
}
\examples{
udpcosine_sum(c(0.44, -0.35, 0.56, -0.21, 0.36))
plot(udpcosine_sum(c(0.44, -0.35, 0.56, -0.21, 0.36)), embellish = "colour")
}
