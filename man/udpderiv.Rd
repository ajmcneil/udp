% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udp-package.R, R/shuffle.R, R/udpcosine.R,
%   R/udplegendre.R, R/vtransforms.R
\name{udpderiv}
\alias{udpderiv}
\alias{udpderiv,shuffle-method}
\alias{udpderiv,udpcosine-method}
\alias{udpderiv,udplegendre-method}
\alias{udpderiv,vtransform-method}
\title{Derivative of a uniform-distribution-preserving transformation}
\usage{
udpderiv(x, u)

\S4method{udpderiv}{shuffle}(x, u)

\S4method{udpderiv}{udpcosine}(x, u)

\S4method{udpderiv}{udplegendre}(x, u)

\S4method{udpderiv}{vtransform}(x, u)
}
\arguments{
\item{x}{an object of class \linkS4class{udp}.}

\item{u}{a vector, matrix or time series with values in \verb{[0, 1]}.}
}
\value{
An object shaped like \code{u} giving \verb{T'(u)}.
}
\description{
Returns \verb{T'(u)} for the \code{\link[=udptrans]{udptrans()}} map of \code{x}, at every point where \code{T} is
differentiable. Every \linkS4class{udp} class in this package is piecewise
smooth with finitely many exceptional points; at those, \code{udpderiv()} returns
the \emph{left} derivative (the one-sided limit from below), except at \code{u = 0},
where only the right derivative exists and is returned instead.
}
\details{
The construction depends on the class of \code{x}:
\itemize{
\item \linkS4class{shuffle}: the slope \code{signs[i]} of the strip containing \code{u}
(\code{1} or \code{-1}), taking the left strip at a strip boundary.
\item \linkS4class{udpcosine}: \code{degree} or \code{-degree}, the sign matching the
direction of the linear piece containing \code{u}, taking the left piece at a
kink.
\item \linkS4class{vtransform}: the analytic gradient already used internally
by \code{\link[=vinverse]{vinverse()}} and \code{\link[=vdownprob]{vdownprob()}}; it returns the lower-branch value at
the fulcrum \code{delta}.
\item \linkS4class{udplegendre}: \verb{T'(u) = f_j(L_j(u)) * L_j'(u)}, where \code{f_j}
is the density of \code{L_j(U)} written as \verb{sum(1 / abs(L_j'(u_i)))} over the
pre-images of \code{L_j(u)} -- computed from the same exact polynomial roots
used by \code{\link[=udpinverse]{udpinverse()}}. At a turning point of \code{L_j} this is \code{0 * Inf} and
is replaced by the exact left derivative, which is always \code{2} or \code{-2}. At
any other pre-image of a turning-point value, \code{T} has a one-sided
vertical tangent; the formula already returns that as \code{Inf} or \code{-Inf}
without a special case.
}
}
\section{Methods (by class)}{
\itemize{
\item \code{udpderiv(shuffle)}: The slope \code{signs[i]} of the strip containing \code{u}
(\code{1} or \code{-1}), taking the left strip at a strip boundary and the right
strip at \code{u = 0}, where there is no left strip.

\item \code{udpderiv(udpcosine)}: \code{degree} or \code{-degree}, the sign matching the direction
of the linear piece containing \code{u}, taking the left piece at a kink and
the right piece at \code{u = 0}, where there is no left piece.

\item \code{udpderiv(udplegendre)}: \verb{T'(u) = f_j(L_j(u)) * L_j'(u)}, where \code{f_j} is the
density of \code{L_j(U)}, \verb{sum(1 / abs(L_j'(u_i)))} over the pre-images of
\code{L_j(u)} found by the same root-finding as \code{\link[=udpinverse]{udpinverse()}}. At a turning
point of \code{L_j} this is \code{0 * Inf}; it is replaced there by the exact left
derivative, \code{2 * m} or \code{-2 * m} according to the sign of \verb{L_j''}, where
\code{m} is the number of turning points sharing that critical value (usually
\code{1}; shifted-Legendre polynomials of even degree are symmetric about
\code{u = 1/2}, so their turning points other than the centre come in mirror
pairs \code{m = 2} with the same critical value). At any other pre-image of a
turning-point value \code{T} has a one-sided vertical tangent, which the
formula already returns as \code{Inf} or \code{-Inf} without a special case.

\item \code{udpderiv(vtransform)}: The analytic gradient stored in the vtransform's
\code{gradient} slot; already returns the lower-branch value at the fulcrum
\code{delta}.

}}
\examples{
udpderiv(shuffle(c(3, 1, 2), signs = c(1, -1, 1)), c(0.1, 0.34, 0.9))
udpderiv(udpcosine(3), c(0.1, 0.3, 0.9))
udpderiv(vsymmetric(), c(0.25, 0.5, 0.75))
udpderiv(udplegendre(3), c(0.1, 0.3, 0.5))
}
