% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shuffle.R
\name{aceshuffle}
\alias{aceshuffle}
\title{Alternating conditional expectation for shuffle transformations}
\usage{
aceshuffle(U, m, maxit = 100L, init1 = NULL, init2 = NULL)
}
\arguments{
\item{U}{a two-column numeric matrix with values in \verb{[0, 1]}.}

\item{m}{the common length of the two permutations (the number of strips).}

\item{maxit}{maximum number of alternating sweeps.}

\item{init1, init2}{optional starting \linkS4class{shuffle} objects with
permutations of length \code{m}; the identity shuffle is used by default.}
}
\value{
An object of class \linkS4class{bishuffle}.
}
\description{
Given a bivariate sample of (approximately) uniform variables,
\code{aceshuffle()} searches for a \code{\link[=shuffle]{shuffle()}} of each column that maximizes
the linear correlation between the transformed columns.
}
\details{
It alternates in the manner of alternating conditional expectations: with the
shuffle of column 2 fixed it selects the shuffle of column 1 maximizing the
correlation, then with that new shuffle of column 1 fixed it selects the
shuffle of column 2, repeating until neither shuffle changes or \code{maxit}
sweeps have been done.

Each selection is solved in closed form. Written as a covariance (a shuffle
leaves the variance of uniform data essentially unchanged), the objective
separates: the sign on each strip is chosen independently, and the
permutation follows from the rearrangement inequality. A sweep therefore
costs \verb{O(n + m log m)}.

The fitted pair is unique only up to the joint reflection
\verb{(shuffle1, shuffle2) -> (1 - shuffle1, 1 - shuffle2)}, which leaves the
correlation unchanged. \code{aceshuffle()} returns the orientation whose shuffles
trend upward, measured for \code{U} uniform by
\code{E[shuffle(U) | U > 1/2] - E[shuffle(U) | U < 1/2]} (how much higher the
shuffle sits on the right half of its domain than the left). It keeps the
orientation with the larger sum of that measure over \code{shuffle1} and
\code{shuffle2}, breaking a tie (for instance under perfect negative dependence,
where the two shuffles cannot both trend upward) on \code{shuffle1} then
\code{shuffle2}. The measure is a plain trend, not a slope at \code{u = 1}: it does
not force \code{shuffle1} to match a particular generating transform when that
transform is close to symmetric about the centre of the unit square.
}
\examples{
set.seed(1)
u1 <- runif(2000)
u2 <- (u1 + 0.5) \%\% 1
fit <- aceshuffle(cbind(u1, u2), m = 4)
fit@correlation
plot(fit, embellish = "colour")
}
