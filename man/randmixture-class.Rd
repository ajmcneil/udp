% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bsicopula.R
\docType{class}
\name{randmixture-class}
\alias{randmixture-class}
\title{Class of mixture-of-copulas randomizer models}
\description{
A randomizer model for \linkS4class{bsicopula} that draws \verb{(Z1, Z2)}
given \verb{(V1, V2)} from one of two copulas, \code{cop1} or \code{cop2}, chosen by a
user-supplied \code{selector(v1, v2)}. Unlike \linkS4class{randsdvine}, this needs
no constraint on \code{selector} to keep \code{udpsi()}'s stochastic-inversion
guarantee valid: whichever of \code{cop1}/\code{cop2} gets picked, it is still some
copula, and every copula's own coordinate margins are uniform on
\verb{[0, 1]} by definition regardless of the other coordinate or the
dependence parameter -- so \verb{Z1 | V1 = v1, V2 = v2} is uniform on
\verb{[0, 1]} for every \verb{(v1, v2)}, whatever \code{selector} does, which is
exactly what \code{Z1} independent of \code{V1} requires.
}
\section{Slots}{

\describe{
\item{\code{cop1,cop2}}{parCopula objects (\pkg{copula}) or bicop_dist objects
(\pkg{rvinecopulib}), the two copulas of \verb{(Z1, Z2)} to choose between.}

\item{\code{selector}}{a function \code{selector(v1, v2)} returning a logical vector
the same length as \code{v1}/\code{v2}: \code{TRUE} selects \code{cop1}, \code{FALSE} selects
\code{cop2}. Any further parameters (such as a threshold) should be
captured in \code{selector}'s closure rather than passed separately.}
}}

\seealso{
\code{\link[=randmixture]{randmixture()}} to construct one; \linkS4class{bsicopula} to use it.
}
