% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/udp-package.R, R/shuffle.R, R/udpcosine.R,
%   R/udplegendre.R, R/vtransforms.R
\name{pcoincide}
\alias{pcoincide}
\alias{pcoincide,udp-method}
\alias{pcoincide,shuffle-method}
\alias{pcoincide,udpcosine-method}
\alias{pcoincide,udplegendre-method}
\alias{pcoincide,vtransform-method}
\title{Probability that stochastic inversion recovers the original value}
\usage{
pcoincide(x)

\S4method{pcoincide}{udp}(x)

\S4method{pcoincide}{shuffle}(x)

\S4method{pcoincide}{udpcosine}(x)

\S4method{pcoincide}{udplegendre}(x)

\S4method{pcoincide}{vtransform}(x)
}
\arguments{
\item{x}{an object of class \linkS4class{udp}.}
}
\value{
A single unnamed probability in \verb{(0, 1]}.
}
\description{
Let \code{U} be uniform on \verb{[0, 1]} and \code{V = udptrans(x, U)}. Feeding \code{V} back
through \code{\link[=udpsi]{udpsi()}} with an independent uniform randomizer returns \emph{some}
pre-image of \code{V}; \code{pcoincide()} is the probability that it is \code{U} itself,
\code{P(udpsi(x, udptrans(x, U)) == U)}.
}
\details{
Write \verb{p_1(v), ..., p_k(v)} for the selection probabilities \code{\link[=udpinverse]{udpinverse()}}
attaches to the pre-images of \code{v}. The uniform-distribution-preserving
property makes the true pre-image \code{U} given \code{V = v} follow that same
distribution, so the chance of drawing it again is the collision probability
\verb{sum_j p_j(v)^2}, and
\deqn{\mathrm{pcoincide}(x) = \int_0^1 \sum_j p_j(v)^2 \, dv.}

The default method evaluates this integral numerically from \code{\link[=udpinverse]{udpinverse()}}.
Classes with a closed form override it: a \linkS4class{shuffle} is a
bijection so the probability is \code{1}; a degree-\code{k} \linkS4class{udpcosine}
has \code{k} equally weighted pre-images so it is \code{1 / k}; a
\linkS4class{vtransform} gives \verb{delta^2 + (1 - delta)^2 + 2 Var(p_down)}.
}
\section{Methods (by class)}{
\itemize{
\item \code{pcoincide(udp)}: Numerically integrate \verb{sum_j p_j(v)^2} using the
selection probabilities from \code{\link[=udpinverse]{udpinverse()}}. Serves any \linkS4class{udp}
class without a closed form.

\item \code{pcoincide(shuffle)}: A shuffle is a bijection, so stochastic inversion
always recovers the original value: the probability is \code{1}.

\item \code{pcoincide(udpcosine)}: The degree-\code{k} triangle wave has \code{k} equally weighted
pre-images at almost every \code{v}, so the probability is \code{1 / k}.

\item \code{pcoincide(udplegendre)}: Integrate \verb{sum_j p_j(v)^2} as in the default method,
but split the range at the images of the turning points of \code{L_j}, where
the integrand has a corner, so each piece is smooth. Accuracy is bounded
by the \code{F_j^{-1}} spline, like the rest of the class.

\item \code{pcoincide(vtransform)}: The two branches of a v-transform carry probabilities
\code{p_down(v)} and \code{1 - p_down(v)}, so the collision integral is
\verb{delta^2 + (1 - delta)^2 + 2 Var(p_down)} with \code{Var(p_down)} found by
numerical integration. The linear and symmetric v-transforms use their
closed forms (\code{p_down} is then constant or symmetric about \code{1/2}).

}}
\examples{
pcoincide(shuffle(c(2, 1, 3)))
pcoincide(udpcosine(4))
pcoincide(vlinear(delta = 0.4))
pcoincide(udplegendre(3))
}
